# Enforce LF for all text so CI (Ubuntu) + Prettier (endOfLine:lf) stay consistent
# across Windows/macOS/Linux checkouts.
* text=auto eol=lf

# Binary assets — never normalize.
*.png binary
*.gif binary
*.jpg binary
*.ico binary
*.woff binary
*.woff2 binary
