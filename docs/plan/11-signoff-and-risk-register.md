# Sign-Off Items & Risk Register

## Items Requiring USER SIGN-OFF Before Build

1. **Acrylic substitute** — static blurred-tint + opacity tokens; wallpaper-sampling host-backdrop dropped. (Phase 0 decision, Phase 7 build.)
2. **Write-restriction divergence — APPROVED (user, 2026).** UWP's block on system32 / `.bat`/`.cmd` associations were sandbox artifacts, not product intent; the rewrite is deliberately _more capable_ (Node fs, no re-imposed block). Approved under the same reasoning as #10 (UWP platform artifacts are not parity requirements). DoD: (a) saving to a path UWP blocked succeeds; (b) opening/associating .bat/.cmd works; (c) session stores absolute paths re-validated on load; (d) one-line release note documents divergence. Genuine OS-permission failures still surface as the normal save-error path.
3. **Perf targets (SR-8)** — cold start ≤2000ms / RAM ≤250MB / installer ≤150MB / 1MB open ≤300ms are honest Electron ceilings, not UWP parity. Confirm acceptable.
4. **CM6 visual deltas — APPROVED (user, 2026).** CodeMirror 6 caret/selection/line-rendering will not be pixel-identical to the UWP RichEditBox; golden-image threshold ≤0.1% is the accepted tolerance. Where CM6 cannot match exactly (caret blink timing, native selection handles, IME candidate UI), the CM6-native behavior is accepted and documented. Editor-surface goldens are subject to R8 (UWP reference captures) before final visual sign-off.
5. **Regex flavor deltas — APPROVED (user, 2026).** Find/replace uses JS RegExp; .NET-only constructs are handled as: RightToLeft (find-previous) via the forward-match-all + pick-last-before-cursor shim (0.F); balancing groups, `\Z`/`\z`, .NET named-group syntax `(?<name>)` differences documented as known divergences. Acceptance = regex-parity fixture passes incl. the RTL reverse-search row; documented flavor gaps are accepted, not blockers.
6. **Smart Copy semantics — APPROVED (user, 2026).** Replicate the ACTUAL source behavior: Smart Copy (setting, default off) trims leading/trailing whitespace from the selection on COPY only (cut never trims). NOT "paragraph expansion" (that was a bundle error corrected against source). DoD: with Smart Copy on, copying a selection with surrounding whitespace places the trimmed text on the clipboard; cut is unaffected.
7. **F5 datetime format — APPROVED (user, 2026).** F5 inserts the date/time using the OS current-culture default format (source behavior via CultureInfo.CurrentCulture), NOT a fixed string. The fixed `"h:mm tt M/dd/yyyy"` format applies ONLY to the `.LOG` auto-timestamp (once per open). DoD: F5 output matches the OS locale's default datetime; `.LOG` first-line auto-stamp uses the fixed format.
8. **Other substitutes (batch sign-off):** compact-overlay → frameless alwaysOnTop; FutureAccessList → re-validated absolute paths (missing = unavailable tab); markdown-it; JS diff lib; middle-click autoscroll → OS-native; jump list → OS recents; share → OS share/clipboard; title bar → `titleBarStyle:hidden`+overlay.
9. **Out-of-scope confirmation:** Game Bar widget, Store/AppCenter telemetry (no-op shims), multi-line handwriting.
10. **File-size cap dropped — APPROVED (user, 2026).** UWP's 1,024,000-byte open limit was an architectural/performance artifact (RichEditBox + sandbox), not product intent. The rewrite drops the hard cap; opening files larger than the original is explicitly acceptable. Deliberate "more capable than 1:1" divergence (same category as #2). DoD: a file above the old boundary opens/edits/saves with 0% byte round-trip mismatch; no hard refusal. Soft safeguards for very large files (defer optional decorations) are allowed but must never refuse the file.

## Scope Ledger (settled)

**IN-SCOPE (full behavior parity):** tabbed editing/SetsView; all editor commands + shortcuts; find/replace (incl. regex + find-previous); encoding read/write/convert + EOL; session snapshot + crash recovery; cross-window full-state drag; status bar (8-column flyouts); all settings panes + live theme + accent; markdown preview + diff viewer (functional); print; 29-locale i18n; file associations + `notepads://` + CLI argv/cwd; multi-instance broker; custom title bar. (NOTE: the UWP 1,024,000-byte file-size cap is intentionally DROPPED — see sign-off item #10, not a parity requirement.)

**IN-SCOPE-WITH-SUBSTITUTE (documented divergence + sign-off):** host-backdrop acrylic → static blurred-tint; compact-overlay F12 → frameless alwaysOnTop; FutureAccessList → absolute paths re-validated; markdown engine → markdown-it; diff engine → JS diff lib; middle-click autoscroll → OS-native; jump list → OS recents; share → OS share/clipboard; title bar → titleBarStyle:hidden+overlay.

**OUT-OF-SCOPE:** Game Bar widget; Store/AppCenter telemetry (no-op shim); multi-line handwriting; RichEditBox internal quirks as such (replicate observable behavior only); Win32 read-only-file write exploit.

## Risk Register

| #   | Riskiest unknown                                                                                              | De-risking spike (Phase 0)                                  | Gate that proves it                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | Rollback / contingency                                                                                                                                                                                                                                                                                   |
| --- | ------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| R1  | **Editor-core behavioral parity** (shadow-buffer offsets, undo granularity)                                   | 0.D — CRLF/LF offset map + 4 undo cases                     | Gate 0 + Gate 3 (line/col match, undo granularity, golden ≤0.1%)                                                                                                                                                                                                                                                                                                                                                                                                                                                                | If CM6 offset model can't match UWP line/col on CRLF, fall back to an EOL-aware position-mapping layer; worst case document divergence for sign-off                                                                                                                                                      |
| R2  | **Encoding heuristic fidelity**                                                                               | 0.E — port confidence ladder, 20-file pilot                 | Gate 3 (0% byte round-trip; ≤2% detection miss)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | If jschardet diverges >2%, pin to chardet or port UTF.Unknown's scorer; detection misses documented-acceptable, round-trip must stay 0%                                                                                                                                                                  |
| R3  | **Cross-window transfer + broker**                                                                            | 0.C — argv/cwd parse + envelope round-trip across 2 windows | Gate 0 + Gate 6 (full-state adopt/release, void cases)                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | Route all transfers through MAIN registry (already the design); degrade to "duplicate-to-target + close-source" if live handle transfer fails                                                                                                                                                            |
| R4  | **Acrylic feasibility**                                                                                       | 0.A decision spike (translucency approach)                  | Gate 0 decision + Gate 7 golden (substitute baseline)                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | Decided as substitute; if static blur underperforms, drop to flat tinted opacity (no blur) — cosmetic only                                                                                                                                                                                               |
| R5  | **.NET regex reverse search**                                                                                 | 0.F — forward-match-all + pick-last shim                    | Gate 0 + Gate 3 regex-parity fixture (incl. RTL row)                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | Shim is the rollback; remaining flavor gaps are documented sign-off items (#5), not blockers                                                                                                                                                                                                             |
| R6  | **PA-8 retrofit risk**                                                                                        | Built into walking skeleton (Phase 1)                       | Gate 1 + re-assert every IPC-adding phase                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | Non-negotiable; if a dependency forces a violation, replace the dependency — build stays broken until clean                                                                                                                                                                                              |
| R7  | **Perf ceiling miss (SR-8)**                                                                                  | Measured continuously from Phase 1                          | Gate 8                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | Red-flag to user (not silent); mitigations: lazy-load panes, V8 snapshot, defer non-critical IPC                                                                                                                                                                                                         |
| R8  | **Golden baselines are self-referential, NOT real UWP captures** (raised Phase 2)                             | —                                                           | Phase-2 goldens captured from OUR rendered tab strip (drift guard only), verified theme-distinct (light meanRGB ~236, dark ~47, hc ~11+Highlight). They do NOT prove 1:1 with the real UWP SetsView.                                                                                                                                                                                                                                                                                                                            | BEFORE final visual sign-off, recapture baselines from the real UWP Notepads SetsView at matching DPI/theme and re-run the diff. Applies to every golden gate (Phase 2/3/7). Tracked as REQUIRES_UWP_REFERENCE.                                                                                          |
| R9  | **Visual-golden theme capture relies on live `emulateMedia` reactivity** (raised Phase 2)                     | —                                                           | Phase-2 visual e2e emulates theme without a `page.reload()`; baselines verified genuinely distinct this round, but a future regression to mount-only theme reading would silently re-introduce the all-light false-green tabs-builder caught (fixed at 5261bd4 era).                                                                                                                                                                                                                                                            | Hardening follow-up: add a pre-diff theme-assertion guard (assert computed dark-surface color / data-theme attr before pixel diff) OR capture each theme in a freshly-reloaded page, so a theme-read regression fails loudly.                                                                            |
| R10 | **OS-theme is MAIN-owned (nativeTheme), so `emulateMedia({colorScheme})` does NOT drive it** (raised Phase 5) | —                                                           | Phase-5 discovered the genuine path: a renderer `emulateMedia` color-scheme is overridden by MAIN's `nativeTheme`→`EvtThemeOsChanged` push (correct PA-8 behavior: renderer never reads `prefers-color-scheme` for OS theme). The real seam is `app.evaluate(() => nativeTheme.themeSource=...)` in MAIN. Consequence: the Gate-4 `statusbar-visual` dark/hc goldens PASS in the full suite (a prior dark spec leaves `nativeTheme` in the right state) but FAIL in isolation — a latent ordering dependency, not a regression. | Hardening follow-up (R10): refactor the Gate-4 `statusbar-visual` specs (and any theme-dependent visual spec) to drive the `driveOsTheme` MAIN seam per-test instead of relying on ambient `nativeTheme` state, so each spec is order-independent and passes in isolation. Gate-5 specs already do this. |

## Phase progress log

- **Phase 1 — DONE** (walking skeleton). Gates: build, PA-8 (bites), typecheck, lint, 8 unit, Playwright open→decode→IPC→CM6→save→bytes round-trip. Independently re-verified by lead.
- **Phase 2 — DONE** (tabs/SetsView). Gate 2 met: keyboard conformance 100% (14 e2e), behavioral matrix (7 e2e: reorder/close-others/close-right/close-saved/rename commit+cancel/copy-path), golden-image ≤0.1% Light/Dark/HC (verified theme-distinct), 75 unit tests, PA-8 clean (bites on fixture), Phase-1 e2e still green — 25/25 e2e total. Independently re-verified by lead. Latent perf bug (ResizeObserver↔dnd loop, ~25s stall) caught by harness e2e and fixed (5261bd4). Open follow-ups: R8 (UWP reference baselines), R9 (theme-capture guard).
- **Phase 3 — DONE** (editor core: commands, find/replace, encoding/EOL). Gate 3 met + INDEPENDENTLY RE-VERIFIED by lead: build/typecheck/lint ✅; PA-8 clean (65 files, fixture bites) ✅; unit 236/236 (20 files) ✅; full e2e 49/49 (no hangs/skips) ✅ — encoding round-trip 0% byte mismatch across a 161-file corpus (UTF-8/UTF-16 ±BOM, GB18030, Shift-JIS, Big5, 40+ ANSI, mixed-EOL normalizing, empty, .LOG) INCLUDING a >1,024,000-byte file proving the cap is dropped (#10); detection miss ≤2% (lenient rows excluded); keyboard conformance 17/17; undo-granularity paste=1/replace-all=1/iterative=N/smart-copy=0; regex-parity 33 incl. RightToLeft shim; Phases 1/2 still green. MAIN owns encoding/EOL authority; renderer never re-derives. 21 atomic commits (e9b3fa3..HEAD).
  - RECOVERY NOTE: the original Phase-3 team lost 3 of 4 members mid-task to a third-party usage/billing limit; lead salvaged all on-disk WIP, fixed 2 real CM6 API bugs + lint, expanded the encoding corpus 47→161 files (verified lossless), and re-formed a lean team to finish. No work lost.
  - REAL BUGS CAUGHT BY THE HARNESS (would have shipped): (1) keyboard/undo e2e were silently HANGING (false-green) because focusEditor clicked a constantly-reflowing contenteditable — fixed via visible-host selector + force-click; (2) **cross-platform redo bug**: `Ctrl+Shift+Z` invoked UNDO on Windows/Electron because CM6's keymap strips Shift on Ctrl+CHAR events (first lookup `modifiers(name,event,!isChar)`) and matches `Ctrl-z`→undo before the `Shift-Ctrl-z` slot — no separate `Mod-Shift-z` binding can win at any precedence. Fixed (27e5e72) with a single `any`-handler at `Prec.highest` routing on `event.shiftKey`, plus removing all other Mod-z/Mod-y bindings (dropped historyKeymap, stripped z/y from defaultKeymap) so nothing claims `Ctrl-z` first. `Ctrl+Y` redo also works (UWP parity bonus).
  - Open follow-ups carried forward: R8 (editor-surface goldens remain self-referential — REQUIRES_UWP_REFERENCE before final visual sign-off), R9 (theme-capture guard — partially addressed: tabs-visual now asserts actual rendered theme before diff, commit 00f544f).
- **Phase 4 — DONE** (File IO / session snapshot + crash recovery / 8-column status bar). Gate 4 met + INDEPENDENTLY RE-VERIFIED by lead: build/lint ✅; PA-8 clean (73 files) ✅; typecheck node+web+e2e ✅; unit 262/262 (22 files) ✅; full e2e 64/64 ✅. All three Gate-4 checklist lines satisfied. (1) **Session parity** (e2e/session-parity.e2e.ts, 4 tests): scripted dirty-kill → restart on the same `NOTEPADS_E2E_USERDATA` restores tab count/order, per-tab dirty flags, caret (selStart/End), scrollTop, encoding, eol, view-mode EXACTLY; identical re-snapshot = no-op write (dirty-checked vs last JSON, UWP `_lastSessionJsonStr` parity); 3-case recovery (no-file / file-no-pending / file+pending); corrupt JSON → loadLast null + backups renamed `*-Corrupted.txt`; clearRecovered cleanup. PA-4 FAL substitute: file missing at restart → tab `unavailable:true` with `filePath` PRESERVED (distinct from untitled `filePath:null`). (2) **Status bar** (StatusBar.tsx, 8 columns): golden ≤0.1% per theme (Light/Dark/HC) + all 8 flyouts via matrix (path reload/copy/open-folder/rename; modification preview/revert; line-col go-to; zoom out/slider/in/reset; EOL CRLF/CR/LF; encoding reopen-with/save-with Unicode+More; col0/col7 placeholders). 25px h, 11px font, 8/4 pad, hover-reveal; consumes pure statusModel.ts + tokens.ts. (3) **External-mod indicator (line 3)**: lead REJECTED the initial "defer to later phase" call — checklist requires the indicator to _behave per matrix_, not merely expose a reload action. Closed: column-0 is a live state machine (fileStatusTracker.ts + useStatusBarModel.ts) mirroring UWP `CheckAndUpdateFileStatusAsync`/`UpdateFileModificationStateIndicator` — polls `window.notepads.file.revalidatePath` on the active file-backed tab (~3s; only active tab; untitled always 'none'), maps vs last-saved mtime: exists:false → renamedMovedDeleted (E9CE), mtime moved → modifiedOutside (E7BA), else none (collapsed); re-baselines/clears on reload + save. PA-8-clean seam `window.__notepadsTest.statusbar.checkFileStatus()` forces a synchronous check. e2e/statusbar-modstate.e2e.ts drives the real seam (rewrite→E7BA → reload→none → delete→E9CE; verified in isolation). MAIN owns all fs (session.ts: versioned `NotepadsSessionData.json` in userData, `{id}-LastSaved`/`{id}-Pending` extension-less backups in `BackupFiles/`, orphan-pruned, `_backups` sidecar stripped pre-IPC); renderer PA-8-clean. `NOTEPADS_E2E_USERDATA` honored in index.ts before app.whenReady. ipc-contract.ts unchanged (SessionApi/revalidatePath pre-stubbed). 8 atomic commits (be9d88f..7afe4d1).
  - PROCESS NOTE: the prior-session usage-limit blocker had cleared; team re-formed (np-next-p4: lane-a session/main + Gate-4 harness, lane-c status-bar + indicator) and completed with NO member deaths. Lead independently re-ran every gate rather than trusting reports — caught and reversed a false "defer" on the external-mod indicator (would have shipped Gate 4 with line 3 unmet) and confirmed the new modstate spec executes (not silently filtered) by running it in isolation.
  - Open follow-ups carried forward: R8 (status-bar goldens self-referential, same as tab-strip — REQUIRES_UWP_REFERENCE before final visual sign-off), R9 (theme-capture guard applied to status-bar goldens via the R9 data-theme pre-diff guard).
- **Phase 5 — DONE** (settings panes + live theme/accent). Gate 5 met + INDEPENDENTLY RE-VERIFIED by lead on the merged tree: build ✅; lint ✅; PA-8 clean (91 files, renderer fs-free) ✅; typecheck node+web+e2e ✅; unit 282/282 (25 files, +20: brandRamp 9 / panes 9 / useSettings 3 + theme) ✅; full e2e 71/71 ✅. All three Gate-5 checklist lines satisfied, with the new specs CONFIRMED executing in the lead's own run (lines #30-36) — not inferred from a count delta. (1) **Persistence + live-affect** (e2e/settings-persistence.e2e.ts #30): a 6-row matrix (showStatusBar, tabIndents, defaultLineEnding, themeMode, tintOpacity, alwaysOpenNewWindow) each live-affects behavior, then SURVIVES a real close→relaunch on the same `NOTEPADS_E2E_USERDATA`. (2) **Live theme/accent, no reload** (e2e/settings-theme-live.e2e.ts #31-33): OS light↔dark↔hc flips the resolved Fluent bucket live (R9 surface guard asserts the rendered surface BEFORE any check); `themeMode` override pins the bucket regardless of OS; a custom accent re-seeds the BrandVariants ramp live (brand token moves). (3) **Settings-pane golden ≤0.1%/theme** (e2e/settings-visual.e2e.ts #34-36): pixelmatch light/dark/hc, baselines confirmed THEME-DISTINCT by the lead (LFS oids c751ff8b/ea9b942b/55144c9e, sizes 52383/52885/60765 B — HC larger as expected), not the all-light false-green pattern. MAIN owns persistence (src/main/settings.ts: `Settings.json` in userData honoring `NOTEPADS_E2E_USERDATA`, deep-merge over `DEFAULT_SETTINGS`, atomic tmp+rename, broadcast `EvtSettingsChanged` to all windows, corrupt→defaults; src/main/theme.ts: `nativeTheme`+`getAccentColor`→`EvtThemeOsChanged`/`EvtThemeAccentChanged`). Renderer (src/renderer/settings + theme) consumes ONLY `window.notepads`, builds the live FluentProvider brand-ramp theme; chrome tokens confirmed 1:1 vs `ThemeSettingsService.cs`. **Lead-gated contract freeze preceded both lanes**: the new `Settings`/`DEFAULT_SETTINGS`/`SettingsApi` surface (+ `EvtSettingsChanged`) was added, verified, and committed (9c9833e) BEFORE spawning Stream A/C — every field/default grounded verbatim in `AppSettingsService.cs`+`ThemeSettingsService.cs`. Commits 9c9833e (contract freeze) → b107f44/1ec8eed/b808062 (Stream A) → f25df1b/5b6ec8f/779e4fd (Stream C) → 8387ef4/df5e581/f624862/af1e0bd (Gate-5 harness).
  - PROCESS NOTE: team scaled mid-phase per user direction ("启动更多的 team members") — added lane-h (test-engineer) as a dedicated Gate-5 harness lane working `e2e/` in parallel with lane-c's `src/renderer/`, so the harness was written against the frozen seam shape WHILE the seam was being built (no tier collision: lane-a=`src/main/`, lane-c=`src/renderer/`, lane-h=`e2e/`). This shortened the critical path.
  - REAL FINDING CAUGHT BY THE HARNESS: OS-theme is MAIN-owned via `nativeTheme`, so `emulateMedia({colorScheme})` does NOT drive it — lane-h identified the genuine `nativeTheme.themeSource` MAIN seam as the only correct driver. Surfaced a latent Gate-4 fragility (R10): `statusbar-visual` dark/hc goldens pass in the full suite but fail in isolation due to ambient `nativeTheme` ordering. Pre-existing (not introduced Phase 5); recorded as R10 hardening follow-up. Gate-5 specs already drive the seam per-test and are order-independent.
  - Open follow-ups carried forward: R8 (settings-pane goldens self-referential, same as tab-strip/status-bar — REQUIRES_UWP_REFERENCE before final visual sign-off), R9 (theme-capture guard — Gate-5 specs apply the surface guard), R10 (drive the `nativeTheme` MAIN seam per-test in Gate-4 statusbar-visual for isolation robustness).
- **Phase 7 — Gate 7 (final UI gate): line 1 GREEN as drift-guard; lines 2/3 status below** (2026-06-07, UI-fidelity pass). Full gate re-verified: format/typecheck(node+web)/lint/pa8(renderer fs-free)/unit 482/482 (48 files)/build all ✅. e2e 95 passed / 3 failed — the 3 are the KNOWN environment-sensitive WM tests (transfer cross-window adopt + 2× window-matrix compact-overlay always-on-top/maximize) that the headless test WM cannot drive; they fail identically on pristine HEAD and in isolation, not a regression.
  - **Line 1 (golden suite ≤0.1% per component per theme)**: GREEN — all 9 goldens (tab-strip/status-bar/settings × light/dark/hc) pass ≤0.1% + the 7 flyout-matrix specs (16 visual tests total). Baselines were RE-CAPTURED 2026-06-07 (user-approved "re-capture as drift-guards", the documented Phase-2..5 re-bless flow) because the prior baselines were STALE: the strip legitimately grew 1867→1930px after the caption-buttons commit (bcca234) + the UWP-faithful tab-label size fix (875b24e), throwing dimension-mismatch.
  - **LFS ROOT CAUSE FOUND + RESOLVED**: the long-standing "unmaterialized LFS pointer" symptom was because the current `.gitattributes` has NO `filter=lfs` rule (only `*.png binary`) — the line `e2e/visual/baselines/**/*.png filter=lfs` the READMEs/risk-register describe is ABSENT (lost in a prior chore/format migration). So committed HEAD blobs were LFS pointer TEXT but checkout never smudged them (no filter) → 129-byte pointer text in the working tree. The re-captured PNGs (232K total / 9 files) now commit as PLAIN BINARY, permanently replacing the broken pointers. At this size no LFS/remote is needed.
  - **R10 hardening applied to tabs-visual** (the carried-forward follow-up): the tab-strip golden spec now drives the MAIN `nativeTheme` seam (`driveOsTheme`) per-case + reloads for HC, instead of `emulateMedia` alone — caught a genuine false-green (the first `tab-strip-light` case had `data-theme="dark"` because no prior test had nudged `nativeTheme`). **R9 guard updated**: the strip is intentionally `transparent` in light/dark post-acrylic (bcca234), so the guard now asserts the strip's `data-theme` attribute + walks to the painted app-shell ancestor for the luminance band (the strip's own bg is no longer a valid probe).
  - **REGRESSION FIXED**: the settings slide-in rework (state-held `transform: translateX(100%)` until a double-rAF flip) raced React re-renders — any reconcile while not-yet-entered rewrote the inline transform back off-screen, clobbering the e2e capture pin and dropping nav clicks (settings-persistence flaked under full-suite load). Replaced with a CSS keyframe (`np-settings-enter`, chrome.css) whose RESTING transform is `translateX(0)` (on-screen), so React never writes an off-screen transform. Same fix pattern as the tab-entrance keyframe (`np-tab-enter`, opacity-only).
  - **Line 2 (toast timing/offset; compact-overlay + full-screen per matrix)**: PARTIAL. Compact-overlay (F12 frameless alwaysOnTop) + full-screen (F11) CODE is present (src/main/compact-overlay.ts, window.ts) and unit-covered, but the 3 e2e matrix assertions fail under the headless WM (always-on-top/maximize state not drivable) — environment, not code. **Toast is the DEFERRED Toaster** (user decision — no notification surface yet); toast timing line is not implemented.
  - **Line 3 (acceptance matrix / keyboard 100% / encoding 0% byte-mismatch / session parity)**: GREEN — all functional e2e outside the 3 WM-environment cases pass.
  - **R8 STILL OPEN**: the re-captured goldens remain SELF-REFERENTIAL drift-guards (our own Chromium render), NOT real UWP SetsView captures. `REQUIRES_UWP_REFERENCE` remains the release-level follow-up before FINAL fidelity sign-off (real UWP capture deemed infeasible on this box: no UWP build/run capability + cross-rasterizer DirectWrite-vs-Chromium cannot meet ≤0.1% against a Chromium boundingBox clip). Drift-guard green ≠ 1:1 UWP parity.
- **Feature-completeness pass + Gate 8 (non-functional) — measured GREEN; packaging deferred** (2026-06-07). A 4-agent feature sweep against the UWP source found the remaining parity gaps; 8 were implemented this pass (plan doc `12-feature-completeness-and-gate8.md`). Full gate re-verified: format / typecheck(node+web) / lint / pa8 (renderer fs-free, 186 files) / unit **509/509** (53 files, +27) / build all ✅. e2e **95 passed / 3 failed** — the SAME 3 known environment-sensitive WM specs (transfer cross-window adopt + 2× window-matrix compact-overlay), no new failures.
  - **W1 — App-level close reminder (DATA-LOSS fix).** UWP `MainPage_CloseRequested` was unported: the window X / Alt+F4 closed dirty tabs silently. Added `WindowConfirmClose` invoke + `EvtWindowCloseRequested` push; `installCloseGuard` in window-factory `preventDefault`s the native close and forwards to the renderer, which runs the flow then calls `confirmClose()` (1:1 with the UWP deferral). New `AppCloseReminderDialog.tsx` (Save All & Exit / Discard & Exit / Cancel, ported `AppCloseSaveReminderDialog_*` keys). `before-quit` disarms the guard so app-level quit (exit-when-last-tab, window-all-closed) is never blocked. **Deviation from UWP, documented:** UWP skips the prompt when session-snapshot is ON (persists instead); renderer session persistence is not yet wired, so we ALWAYS prompt on dirty tabs — prompting can never lose data, silent-close could. (Renderer session-snapshot wiring is a separate open gap: MAIN/preload have the API, the renderer never calls `session.snapshot`.)
  - **W2 — Read-only + transient-lock write retry.** `writeFileWithRetry` (3 attempts, linear backoff) retries EBUSY/EPERM/EACCES (OneDrive/AV/indexer locks; UWP `ExecuteFileIOOperationWithRetries`); non-transient errors throw immediately. A read-only target exhausts retries then surfaces a descriptive `Result` error (the close-reminder Save branch already aborts on `!ok` — no silent loss). 5 unit tests (injected writer).
  - **W3 — Window bounds persistence.** UWP got size/placement from the OS shell; the rewrite hardcoded 1100×720. New `window-bounds.ts` persists `{x,y,width,height,isMaximized}` to `WindowBounds.json` (atomic, e2e-override-aware), restored before first paint, clamped to a connected display (stale off-screen position dropped, size kept). **No-op under `NOTEPADS_E2E`** AND `restoredWindowOptions()` returns null there → factory keeps its original `show()`-only path so the default window stays pixel-identical (the visual goldens depend on this — a `setSize`+`center` regression shifted the strip 1930→1904px and was caught + fixed). 9 unit tests for the pure clamp/serialize.
  - **W4 — Go-to-line dialog.** Replaced the raw `window.prompt` (Ctrl+G) with a themed Fluent `GoToLineDialog` (ported `GoTo_*` keys, numeric validation + line-count clamp), 1:1 with UWP `GoToControl`. 6 component tests + the `editor-keyboard` Ctrl+G e2e rewritten to drive the dialog.
  - **W5 — Editor right-click context menu.** UWP `TextEditorContextFlyout` was unported (commands existed only as keybindings; Share + RTL had NO UI entry point). New `EditorContextMenu.tsx` mounts a CM6 `domEventHandlers.contextmenu` seam + positioned Fluent menu: Cut/Copy/Paste/Undo/Redo/Select All/RTL/Word Wrap/Search-in-web(selection)/Toggle Preview(md)/Share — restoring Share + RTL entry points. Clipboard via `navigator.clipboard` (DOM API — PA-8 clean).
  - **W6 — About version from package.json.** `vite define __APP_VERSION__ = pkg.version`; `aboutInfo.ts` consumes it (was hardcoded `'0.0.0'`). Wiring is correct; value tracks the package on release bump.
  - **W7 — Real OS ANSI code page.** Encoding system-ANSI fallback was hardcoded windows-1252. New `system-codepage.ts` reads the ACP from the registry (`reg query …\Nls\CodePage\ACP` via child_process — MAIN-only) once at startup, mapped to an iconv codec via the existing ANSI table; falls back to 1252 (non-win32 / read failure). **NOTE: koffi `GetACP()` was tried first but the rolldown build cannot bundle koffi's native `.node` (UNLOADABLE_DEPENDENCY) — `win-corners.ts` only "works" because it is dead code never imported. The `reg query` approach is dependency-free and bundles cleanly.** 3 unit tests.
  - **W8 — Jump List "New window" task.** `app.setUserTasks` adds the UWP `notepads://newinstance` taskbar task (win32-only, best-effort).
  - **DEFERRED (user decision / release infra), recorded as R-items:**
    - **In-app updates (R11):** no electron-updater — needs a publish target (GitHub Releases), code-signing, and an update feed, none of which exist on this box and none end-to-end-verifiable here. UWP relied on Store updates.
    - **Code-signing + branded icon:** the installer is built UNSIGNED with the default Electron icon (no cert / icon asset on this box). Both are release-infra follow-ups, not blockers for the size/functional gate.
    - **Print preview:** kept the @media-print host + OS print-dialog preview (accepted Electron substitution; no custom paginated/continuation layout). User decision.
  - **GATE 8 — non-functional ceilings: ALL FOUR MEASURED GREEN.** Runtime metrics via `scripts/measure-nonfunctional.mjs` (`yarn measure`; launches the built app via Playwright `_electron`, samples `app.getAppMetrics`, times a 1 MB `file.open` IPC round-trip): **cold start 492 ms** (≤2000), **idle RAM 164 MB** (≤250), **1 MB open 27 ms** (≤300). Installer: electron-builder 26.15.0 now installed; `electron-builder.yml` given the `files`/`directories`/`asarUnpack` mapping for the `out/` 3-tier layout (koffi `.node` unpacked from asar), `yarn dist` added → NSIS `Notepads Setup 0.0.0.exe` = **121 MB** (≤150). PA-8 re-asserted clean. Remaining: code-signing + branded icon (release infra), git `gate/*` release tags (pending commit decision).
